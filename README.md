# Mukesh Cold Drink — Feedback System

1. Place the provided logo at `public/assets/mukesh-logo.png`.
2. Copy repo to server.
3. Create .env from .env.example and update MONGO.
4. Install: `npm install`.
5. Start: `npm start`.
6. Visit: `https://yourdomain.com/` for feedback form and `/admin.html` for admin.

Deploy: Build Docker image and push to your host.
Security: Protect `/admin/feedbacks` with basic auth or IP whitelisting and use HTTPS.